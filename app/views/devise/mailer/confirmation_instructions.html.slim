p Welcome #{@email}!
p You can confirm your account email through the link below:
p = render partial: 'components/email_button', locals: { url: confirmation_path(@resource, confirmation_token: @token), label: 'Confirm my account' }
